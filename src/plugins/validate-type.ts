const ValidateTypes = {
  firstMountedRequired: 'firstMountedRequired',
  required: 'required',
  email: 'email',
  regex: 'regex',
  length: 'length',
  minLength: 'minLength',
  maxLength: 'maxLength'
}

export default ValidateTypes
