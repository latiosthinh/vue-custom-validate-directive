const ValidateTypes = {
  onMountedRequired: 'onMountedRequired',
  required: 'required',
  email: 'email',
  regex: 'regex',
  length: 'length',
  minLength: 'minLength',
  maxLength: 'maxLength'
}

export default ValidateTypes
